# GOOGLE SHEETS CREDENTIALS
# Copy nội dung service account JSON vào biến này
GOOGLE_CREDS_JSON='{"type": "service_account", "project_id": "...", ...}'

# HOẶC tạo file credentials.json trong thư mục
